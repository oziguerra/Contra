<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.contra">

    <application
        android:allowBackup="true"
        android:icon="@drawable/clogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/clogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MatchView"></activity>
        <activity android:name=".TournamentsActivity" />
        <activity android:name=".AddPlayerActivity" />
        <activity android:name=".Select_Player" />
        <activity android:name=".fourP_Tournamet" />
        <activity android:name=".T_Creator" />
        <activity android:name=".PlayersActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>